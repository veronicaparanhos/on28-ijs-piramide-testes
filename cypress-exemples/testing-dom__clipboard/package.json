{
  "name": "clipboard",
  "version": "1.0.0",
  "description": "Copy / paste text example",
  "private": true,
  "scripts": {
    "cypress:open": "npx cypress open",
    "cypress:run": "npx cypress run",
    "start": "echo nothing to start",
    "test:ci": "npm run cypress:run",
    "test:ci:chrome": "npx cypress run --browser chrome",
    "test:ci:chrome:headless": "npx cypress run --browser chrome --headless",
    "test:ci:record": "npm run cypress:run -- --record",
    "test:repeat": "npx cypress-repeat -n 5"
  },
  "devDependencies": {
    "cypress": "^13.3.2"
  }
}
